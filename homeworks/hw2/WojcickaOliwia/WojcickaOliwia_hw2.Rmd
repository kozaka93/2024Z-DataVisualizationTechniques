---
title: "Praca domowa 2"
author: "Oliwia Wojcicka"
output: 
  html_document:
    toc: true
    toc_float: true
    code_folding: hide
    theme: united

---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(tidyr)
library(ggplot2)
df <- read.csv("house_data.csv")
```

## Dane 
Dane wykorzystane w Pracy domowej 2 opisuj?? wycen?? nieruchmo??ci. Ka??da obserwacja jest pewn?? nieruchmo??ci??, ktora posiada informacje takie jak powierzchnia, liczba pokoi, liczba ??azienek i wiele wi??cj. Opis wszystkich kolumn w pliku `README.md`.


## Zadanie 1

Jak wygl??da rozk??ad ceny nieruchomo??ci w poszczegolnych kodach pocztowych na przestrzeni lat? Rozwa??amy kody pocztowe, ktore maj?? wi??cej ni?? 280 posiad??o??ci.

```{r zadanie 1, message=FALSE}

df1 <- df %>% 
  mutate(year = substr(date, start = 1, stop = 4)) %>% 
  group_by(zipcode) %>% 
  mutate(how_many = n()) %>% 
  filter(how_many > 280) %>% 
  ungroup() %>%
  group_by(zipcode, year) %>% 
  summarise(mean_price = mean(price)) 
  
g1 <- ggplot(df1, aes(y = as.factor(zipcode), x = mean_price, fill = as.factor(year)))+
  geom_col(position = "dodge") +
  scale_x_continuous(labels = scales::comma)+
  labs(y = "Kod pocztowy",
       x = "??rednia cena (USD)",
       fill = "Rok",
       title="Rozk??ad ??redniej ceny nieruchomo??ci na przestrzeni lat",
       subtitle = "w poszczegolnych kodach pocztowych")+
  theme_minimal()
       
```

**Komentarz: Mamy dane tylko z 2014 i 2015 roku. Mo??emy zauwa??y??, ??e dla wi??kszo??ci kodow pocztowych ceny nieruchomo??ci s?? bardzo podobne lub nieco wy??sze dla 2015 roku. Jedynie dla kodow pocztowych 98004, 98006 ceny w 2015 roku s?? sporo wi??ksze od cen z 2014 roku. W dodatku dla kodow: 98146, 98144, 98056 ceny w 2015 s?? ni??sze ni?? w 2014, ale jest to niewielka ro??nica.**

## Zadanie 2

Czy jest ro??nica w rozk??adzie ceny nierchomo??ci od powierzchni mieszkalnej uwzgl??dniaj??c podzia?? na nieruchomo??ci z piwnic?? i bez?

```{r zadanie 2, message=FALSE}

df2 <- df %>% 
  mutate(has_basement = if_else(sqft_basement == 0, FALSE, TRUE))

g2 <- ggplot(df2, aes(x = sqft_living, y = price, color = as.factor(has_basement)))+
  geom_point(alpha = 0.6) +
  geom_smooth(data = subset(df2, has_basement == TRUE), 
              method = "lm", se = FALSE, color = "blue", linetype = "solid") + 
  geom_smooth(data = subset(df2, has_basement == FALSE), 
              method = "lm", se = FALSE, color = "red", linetype = "solid") +
  scale_y_continuous(labels = scales::comma)+
  labs(x = "Powierzchnia mieszkalna (stopy kwadratowe)", 
       y = "Cena nieruchomo??ci (USD)", 
       color = "Z piwnic??", 
       title = "Rozk??ad ceny nieruchomo??ci od powierzchni mieszkalnej",
       subtitle = "z podzia??em na nieruchomo??ci: z piwnic?? i bez piwnicy") +
  annotate("text", x = 0, y = 7800000, label = "Linia regresji: Z piwnic??", color = "blue", size = 4, hjust = 0) + 
  annotate("text", x = 0, y = 7400000, label = "Linia regresji: Bez piwnicy", color = "red", size = 4, hjust = 0) + 
  theme_minimal()
  

```

**Komentarz: Tak, jest ro??nica w rozk??adzie ceny nieruchomo??ci od powierzchni mieszkalnej uwzgl??dniaj??c podzia?? na nieruchomo??ci z piwnic?? i bez. Mo??emy zauwa??y??, ??e dla bardzo ma??ych mieszka??, ceny mieszka?? bez piwnicy s?? minimalnie wi??ksze. Nast??pnie od powierzchni oko??o 2000 (stop kwadratowych) ceny mieszka?? z piwnic?? s?? wy??sze ni?? bez piwnicy. Warto te?? podkre??li??, ??e od powierzchni oko??o 7600 (stop kwadratowych) nie wyst??puj?? mieszkania bez piwnic.**

## Zadanie 3

Jaka jest zale??no???? pomi??dzy jako??ci?? wyko??czenia nieruchomo??ci a liczb?? pi??ter? 

```{r zadanie 3, message=FALSE}

df3 <- df %>% 
  group_by(floors, grade) %>% 
  summarise(frequency = n(), .groups = "drop") 

g3 <- ggplot(df3, aes(x = as.factor(floors), y = grade, size = frequency))+
  geom_point(alpha = 0.6, color = "blue") +
  scale_size_continuous(range = c(2, 10), breaks = c(10, 100, 1000), labels = c("Ma??a: 0-100", "??rednia: 100-1000", "Du??a: 1000+")) +
  geom_smooth(method = "lm", aes(group = 1), se = FALSE, color = "red", linetype = "solid", show.legend = FALSE) + 
  labs(title = "Zale??no???? pomi??dzy jako??ci?? wyko??czenia nieruchomo??ci a liczb?? pi??ter",
       y = "Jako???? wyko??czenia",
       x = "Liczba pi??ter",
       size = "Liczba odpowiedzi") +
   annotate("text", x = 0, y = 14, label = "Linia regresji ", color = "red", size = 4, hjust = 0) +
  theme_minimal()
  

```

**Komentarz: Ogolnie mowi??c im wi??ksza liczba pieter, tym wy??sza jako???? wyko??czenia. Warto jednak zauwa??y??, ??e danych dla du??ej ilo??ci pi??ter (3/3,5) jest znacznie mniej danych ni?? dla mniejeszj liczby pi??ter. Ale ??rednia jako??ci, ze wszystkich dost??pnych danych, ro??nie wraz ze wzrostem liczby pi??ter.**

## Zadanie 4
Jaka jest zale??no???? mi??dzy powierzchni?? mieszkaln?? nieruchomo??ci a powierzchni?? gruntu w podziale na liczb?? sypialni?

```{r zadanie 4, message=FALSE}

df4 <- df %>% 
  group_by(bedrooms) %>% 
  summarise(mean_sqft_living = mean(sqft_living), mean_sqft_lot = mean(sqft_lot), .groups = "drop") %>% 
  pivot_longer(cols = c(mean_sqft_living, mean_sqft_lot), 
               names_to = "area_type", 
               values_to = "average_area")

g4 <- ggplot() +
  geom_col(data = df4 %>% filter(area_type == "mean_sqft_lot"), 
           aes(x = as.factor(bedrooms), y = average_area, fill = "mean_sqft_lot"), 
           alpha = 0.9) + 
  geom_col(data = df4 %>% filter(area_type == "mean_sqft_living"), 
           aes(x = as.factor(bedrooms), y = average_area, fill = "mean_sqft_living"), 
           alpha = 1) + 
  labs(title = "??rednia powierzchnia mieszkalna i gruntowa w zale??no??ci od liczby pokoi",
       x = "Liczba sypialni",
       y = "??rednia powierzchnia (stopy kwadratowe)",
       fill = "Typ powierzchni") +
  scale_fill_manual(values = c("mean_sqft_living" = "blue", "mean_sqft_lot" = "brown"),
                    labels = c("mean_sqft_living" = "Powierzchnia Mieszkalna", 
                               "mean_sqft_lot" = "Powierzchnia Gruntowa"))
  theme_minimal()

```

**Komentarz: Mo??emy zauwa??y??, ??e powierzchnia gruntowa jest wi??ksza od powierzchni mieszkalnej, dla ka??dej liczby sypialni. Dla mieszka?? o liczbie sypialni mniejszej od 8, powierzchnia gruntowa jest kilkukrotnie wi??ksza od powierzchni mieszkalnej. Natomiast gdy liczba pokoi jest w przedziale 8-11, to wtedy powierzchnia gruntowa jest maksymalnie dwa razy wi??ksza od mieszkalnej. Mamy na wykresie jedn?? odstaj??c?? warto??c liczby pokoi, czyli 33. Dla niej powierzchnia mieszkalna jest 3 razy mniejsza od gruntowej. **

## Zadanie 5
Rozwa??my tylko nieruchomo??ci, ktore mia??y remont. W ktorej dekadzie by??o najwi??cej przeprowadzonych remontow? Czy w poszczegolnych dekadach jest porownywalny procent nieruchmo??ci dla ka??dej warto??ci miary jako??ci i wyko??czenia nieruchomo??ci?

```{r zadanie 5, message=FALSE}
#wykres g5 - odpowiedz na 1 pytanie
#wykres g55 - odpowiedz na 2 pytanie

df5 <- df %>% 
  filter(yr_renovated != 0) %>% 
  mutate(decade = floor(yr_renovated / 10) * 10) %>% 
  group_by(decade) %>% 
  summarise(how_many = n(), .groups = "drop")

g5 <- ggplot(df5, aes(x = decade, y = how_many))+
  geom_col()+
  scale_x_continuous(breaks = df5$decade) +
  labs(x = "Dekada", y = "Liczba Remontow", title = "Liczba Remontow w Poszczegolnych Dekadach") +
  theme_minimal()

df55 <- df %>% 
  filter(yr_renovated != 0) %>% 
  mutate(decade = floor(yr_renovated / 10) * 10) %>% 
  group_by(decade, grade) %>% 
  summarise(with_grade = n(), .groups = "drop") %>% 
  inner_join(df5, by = "decade") %>% 
  mutate(percent = round(with_grade/how_many *100, 1)) 

g55 <- ggplot(df55, aes(x = as.factor(grade), y = percent, fill = as.factor(grade))) +
  geom_bar(stat = "identity", position = "dodge") + 
  facet_wrap(~ decade) +
  labs(x = "Grade", y = "Procent", title = "Procent Nieruchomo??ci wed??ug miary jako??ci i wyko??czenia w Ro??nych Dekadach") +
  scale_x_discrete(labels = NULL) +  
  scale_fill_manual(name = "Miara", 
                    values = c("4" = "pink", "5" = "magenta", 
                               "6" = "purple", "7" = "blue", 
                               "8" = "lightblue", "9" = "lightgreen", 
                               "10" = "darkgreen", "11" = "brown", 
                               "12" = "red", "13" ="darkorange"))+
  theme_minimal() +
  theme(legend.position = "right")


```

**Komentarz: W dekadzie 2000 by??o najwi??cej remontow. W poszczegolnych dekadach zdecydowanie nie jest porownywalny procent nieruchmo??ci dla ka??dej warto??ci miary jako??ci i wyko??czenia nieruchomo??ci. Mo??emy zauwa??y??, ??e np. w dekadzie 1930 mamy zale??no??c tylko od jednej miary rownej 6. W dekadach 1960, 1970 procent miary rownej 7 jest zdecydowanie wy??szy od pozosta??ych. W dekadach 1980, 1990, 2000 wyst??powanie najwyzszych miar (11,12,13) jest znikome w porownaniu do pozosta??ych. Podsumowuj??c, dla ??adnej dekady nie mo??na powiedzie??, ze procent nieruchomo??ci jest porownywalny dla ka??dej miary.**

## Zadanie 6

Jak prezentuje si?? warto???? minimalna, ??rednia i maksymalna ceny nieruchmo??ci w podziele na rok budowy nieruchomo??ci.

```{r zadanie 6, message=FALSE}

df6 <- df %>% 
  group_by(yr_built) %>% 
  summarise(min_price = min(price), max_price = max(price), mean_price = mean(price)) %>% 
  pivot_longer(cols = c(min_price, max_price, mean_price), 
               names_to = "price_type", 
               values_to = "price")
  

g6 <- ggplot(df6, aes(x = as.factor(yr_built), y = price, fill = price_type)) +
  geom_col(position = "stack") +  # U??ycie "stack" do nak??adania s??upkow
  labs(title = "Cena nieruchomo??ci w zale??no??ci od roku budowy",
       x = "Rok",
       y = "Cena (USD)",
       fill = "Rodzaj ceny") +
  scale_x_discrete(breaks = seq(1900, 2015, by = 10)) +
  scale_y_continuous(labels = scales::comma) +
  scale_fill_manual(values = c("min_price" = "yellow", 
                                "mean_price" = "orange", 
                                "max_price" = "red"),  
                     labels = c("min_price" = "Minimalna", 
                                "mean_price" = "??rednia", 
                                "max_price" = "Maksymalna")) +
  theme_minimal() +
  theme(legend.position = "right")

```

**Komentarz: Warto??ci minimalnych i ??rednich cen nieruchmo??ci s?? porownywalne na przestrzeni lat. Natomiast warto??ci ceny maksymalnej ro??ni?? si?? dla poszczegolnych lat budowy. Mo??emy zauwa??y??, ??e w trzech latach (1910, 1940, 2001) maksymalne ceny s?? znacz??co wy??sze ni?? w pozost??aych. Najwy??sza cena maksymalna jest osi??gana w 1910 roku i wynosi oko??o 8500000 usd.**