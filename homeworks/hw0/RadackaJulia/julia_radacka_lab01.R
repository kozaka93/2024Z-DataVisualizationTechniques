proton()

employees[employees['surname'] == 'Insecure']

proton(action = "login", login = "johnins", password=top1000passwords[3])

sapply(top1000passwords,proton)
lista[lista != NULL]

for(password in top1000passwords){
  proton(action = "login", login = "johnins", password=password)
}

logs['login'== '']

employees[employees['surname'] == "Pietraszko"]

logos <- logs[logs$login =='slap',]

aggregate(logos,logos['host'],length)

proton(action = 'server', host = "194.29.178.16")

bash_history
